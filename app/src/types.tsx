
export interface ButtonProps {
    children: React.ReactNode;
    onClick?: () => void;
    className?: string;
  }
  
  export interface ComfortFoodProps {
    title: string;
    description: string;
    imageSrc: string;
    imageAlt: string;
  }
